{
  "homepage": "https://shivankursharma018.github.io/v2/",
  "name": "v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "predeploy": "npm run build",
    "build": "webpack",
    "dev": "webpack serve",
    "deploy": "git --work-tree dist add dist -f && git --work-tree dist commit -m 'Deploy to gh-pages' && git push origin `git subtree split --prefix dist main`:gh-pages --force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.0"
  }
}
